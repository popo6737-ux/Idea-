# Po⁴ (포포) — 프로젝트 컨텍스트

야간 골프 재귀반사 나노 코팅 솔루션. 골프공 표면에 초박막 재귀반사 코팅을 입혀, 빛이 닿는 순간 일반 공보다 압도적으로 밝게 반사되게 한다.

---

## 핵심 포지셔닝 (현장 테스트 후 확정)

**Before (초기 기획):** "어둠 속에서도 보인다"
**After (현장 테스트 반영):** "빛이 닿는 순간, 일반 공보다 300% 밝게"

재귀반사(Retro-reflection)는 빛이 입사한 방향으로만 강하게 반사된다. 완전 암부에서는 효과가 제한적이다. 실제 야간 골프장 테스트에서 확인된 사실이며, 이를 솔직하게 명시한 채로 포지셔닝을 잡는다.

**타겟 유스케이스:** 카트 헤드라이트 / 스마트폰 플래시 / 코스 조명이 있는 환경에서 러프 속 공 탐색

---

## 현장 테스트 영상 분석 (2026-05)

파일: `IMG_1471.MOV` (9.4초, 1920×1080, 실제 야간 골프장 촬영)

### 관찰 결과
- 두 공 나란히 배치: 왼쪽 일반 공 / 오른쪽 프로토타입
- 프레임 9~11에서 프로토타입에 분홍/핑크 색조 확인 → 재귀반사 유리 미소구체의 파장 집중 반사 특성
- 밝기 차이가 극적이지 않은 이유: 코스 ambient light가 두 공 모두를 밝히고 있어 재귀반사 효과가 희석됨

### 영상의 한계
| 문제 | 원인 |
|---|---|
| 극적 밝기 차이 없음 | 주변 조명이 일반 공도 밝힘 |
| 색차(핑크 tint)만 보임 | 광원이 재귀반사 임계각에 정확히 미달 |
| 핸드헬드 흔들림 | 카트 위 촬영 추정 |

### 활용 전략
현재 영상을 버리지 않는다. "솔직한 창업자" 내러티브로 역이용:
> "1차 필드 테스트 영상 — 색차는 확인됐으나 밝기 차이는 기대 이하. 광원 각도 최적화 진행 중."

---

## 보완 영상 촬영 플랜

### 목표
"같은 공, 같은 빛, 다른 밝기"를 30초 안에 극적으로 보여주기

### 촬영 설정
- **환경:** 소등 가능한 실내 연습장 잔디 매트 or 조명 없는 야간 러프
- **장비:** 삼각대 고정, 두 공 나란히, 카메라에서 3~5m 거리
- **광원 연출 순서:**
  1. 플래시 OFF (5초) → 두 공 모두 어둡고 비슷함
  2. 플래시 ON (5초) → 프로토타입만 압도적으로 빛남
  3. 카메라 좌우 이동 → 재귀반사 특성(광원 방향에서만 밝음) 자연 노출
- **편집:** 슬로우모션 0.5× (플래시 ON 순간), "← 일반 공 / Po⁴ →" 자막

### 이후 웹사이트 반영 계획
1. Hero 섹션에 `<video autoplay muted loop playsinline>` 임베드 (before/after 클립)
2. 솔루션 섹션에 비교 이미지 (현재 영상 frame_009, frame_010 활용)
3. "필드 테스트 로그" 섹션 추가 (날짜/장소/조건/결과/다음 과제)

---

## 실행 우선순위

```
1. 보완 영상 촬영 (소등 환경 확보)
2. 웹사이트 영상 임베드 + 비교 이미지 섹션 추가
3. 현재 영상 + "개선 중" 코멘트로 SNS 1차 업로드 (바이럴 씨앗)
4. 보완 영상 완성 후 와디즈 크라우드펀딩 캠페인 메인 영상으로 활용
```

---

## 프로젝트 구조

```
Idea-/
├── index.html          # 단일 파일 웹사이트 (다크 테마, 바닐라 JS)
├── server.js           # Express 5 서버 (REST API + static serve)
├── package.json
├── Dockerfile          # node:22-alpine, node_modules 호스트에서 COPY
├── .dockerignore       # .git, *.md 제외 (node_modules는 포함)
├── .gitignore          # node_modules/, data/ 제외
├── data/               # JSON 파일 스토리지 (gitignore됨)
│   ├── registrations.json
│   └── inquiries.json
└── 사업계획서_Po4_야간골프_나노코팅.md
```

## API 엔드포인트

| Method | Path | 설명 |
|---|---|---|
| POST | /api/register | 얼리어답터 사전등록 (name, email, phone, rounds_per_month) |
| GET | /api/register/count | 현재 등록자 수 반환 |
| POST | /api/inquiry | 프로샵 제휴 문의 (company, name, email, phone, message) |

## 실행 방법

```bash
# 로컬 개발
npm run dev        # node --watch server.js

# Docker
docker build -t po4 .
docker run -p 3000:3000 -v $(pwd)/data:/app/data po4
```

## 주요 기술 결정 사항

- **Express 5**: wildcard 라우트는 `'/{*path}'` 사용 (`'*'`는 PathError 발생)
- **node_modules COPY**: Docker 빌드 시 npm install 대신 호스트 node_modules를 COPY (SSL 인증서 이슈 우회)
- **JSON 파일 스토리지**: MVP 단계 외부 DB 불필요, `data/` 디렉토리에 저장

## 브랜드 원칙

- 제약·바이오 QC 출신 등 직업적 배경 언급 금지 (제거 완료)
- "어둠 속에서도 보인다" 류의 과장 문구 금지
- 재귀반사의 물리적 제한(완전 암부 효과 제한)은 항상 투명하게 명시
- 색상: Gold `#c9a84c`, Dark `#0a0e14`

## Git

- 작업 브랜치: `claude/night-golf-service-plan-KuFaB`
- PR #1: 위 브랜치 → main (머지 완료)
- 이후 작업은 동일 브랜치에서 진행 후 main에 직접 머지
